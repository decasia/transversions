<h1>{{model.name}}</h1>

<h3>Definition</h3>
<div>
  {{#mobiledoc-editor
      atoms=(mobiledoc-atoms-list)
      cards=(mobiledoc-cards-list)
      mobiledoc=model.definition as |editor|
  }}
    <div class="btn-toolbar">
      <div class="btn-group">
        {{#mobiledoc-markup-button editor=editor for="strong" classNames="btn btn-default btn-sm"}}
          <span class="glyphicon glyphicon-bold" aria-hidden="true"></span>
        {{/mobiledoc-markup-button}}
        {{#mobiledoc-markup-button editor=editor for="em" classNames="btn btn-default btn-sm"}}
          <span class="glyphicon glyphicon-italic" aria-hidden="true"></span>
        {{/mobiledoc-markup-button}}
      </div>
    </div>
  {{/mobiledoc-editor}}
</div>

<h3>Example</h3>
<div>
  {{#mobiledoc-editor
      atoms=(mobiledoc-atoms-list)
      cards=(mobiledoc-cards-list)
      mobiledoc=model.example as |editor|
  }}
    <div class="btn-toolbar">
      <div class="btn-group">
        {{#mobiledoc-markup-button editor=editor for="strong" classNames="btn btn-default btn-sm"}}
          <span class="glyphicon glyphicon-bold" aria-hidden="true"></span>
        {{/mobiledoc-markup-button}}
        {{#mobiledoc-markup-button editor=editor for="em" classNames="btn btn-default btn-sm"}}
          <span class="glyphicon glyphicon-italic" aria-hidden="true"></span>
        {{/mobiledoc-markup-button}}
      </div>
    </div>
  {{/mobiledoc-editor}}
</div>

<h3>Discussion</h3>
<div>
  {{#mobiledoc-editor
      atoms=(mobiledoc-atoms-list)
      cards=(mobiledoc-cards-list)
      mobiledoc=model.discussion as |editor|
  }}
    <div class="btn-toolbar">
      <div class="btn-group">
        {{#mobiledoc-markup-button editor=editor for="strong" classNames="btn btn-default btn-sm"}}
          <span class="glyphicon glyphicon-bold" aria-hidden="true"></span>
        {{/mobiledoc-markup-button}}
        {{#mobiledoc-markup-button editor=editor for="em" classNames="btn btn-default btn-sm"}}
          <span class="glyphicon glyphicon-italic" aria-hidden="true"></span>
        {{/mobiledoc-markup-button}}
      </div>
    </div>
  {{/mobiledoc-editor}}
</div>
